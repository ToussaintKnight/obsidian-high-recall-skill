# World Models and Simulation

This fixture note covers simulation and world-model workflows for robot training. It mentions synthetic data, sim-to-real evaluation, environment models, policy rollouts, robot simulators, and embodied world models.

Relevant phrases: world model, simulation, simulator, synthetic data, sim-to-real, robot training, environment model.

The note is intentionally small so CI can verify evaluator behavior without a private vault.
